name: Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: mihails-strasuns/setup-dlang@v0.3.0
      name: Install DMD
    - name: Build
      run: dmd ctrlc.d config.d utils.d && dmd ctrlp.d config.d utils.d

